{
  "name": "secure-password-app",
  "version": "1.0.0",
  "description": "Secure password validation application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "sonar": "sonar-scanner"
  },
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3",
    "sonarqube-scanner": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}